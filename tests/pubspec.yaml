name: tests
description: A an app for proxy_setting testing (e2e).
publish_to: "none"
version: 0.0.0

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  http: ^0.13.4
  riverpod: ^1.0.3
  flutter_riverpod: ^1.0.3

  detect_proxy_setting:
    path: ../packages/detect_proxy_setting
  proxy_setting_macos:
    path: ../packages/proxy_setting_macos
  proxy_setting_windows:
    path: ../packages/proxy_setting_windows
  proxy_setting_platform_interface:
    path: ../packages/proxy_setting_platform_interface

dependency_overrides:
  detect_proxy_setting:
    path: ../packages/detect_proxy_setting
  proxy_setting_macos:
    path: ../packages/proxy_setting_macos
  proxy_setting_windows:
    path: ../packages/proxy_setting_windows
  proxy_setting_platform_interface:
    path: ../packages/proxy_setting_platform_interface

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
