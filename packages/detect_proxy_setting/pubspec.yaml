name: detect_proxy_setting
description: Flutter plugin for detect proxy setting
repository: https://github.com/ueki-tomohiro/proxy_setting/tree/main/packages/proxy_setting
version: 0.0.2

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.5.0"

flutter:
  plugin:
    platforms:
      ios:
        default_package: proxy_setting_ios
      macos:
        default_package: proxy_setting_macos
      windows:
        default_package: proxy_setting_windows

dependencies:
  flutter:
    sdk: flutter
  proxy_setting_ios:  ^0.0.2
  proxy_setting_macos:  ^0.0.2
  proxy_setting_platform_interface:  ^0.0.2
  proxy_setting_windows: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.0
  plugin_platform_interface: ^2.0.0
  test: ^1.16.3
